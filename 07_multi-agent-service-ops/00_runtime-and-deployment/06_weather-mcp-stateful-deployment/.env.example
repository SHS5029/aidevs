# 로컬에서만 사용하는 compose.infrastructure.yml(PostgreSQL·Redis)의 값입니다.
# Application Container의 환경 변수는 .env.docker.example을 복사해 .env.docker에 입력합니다.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=agent_db
